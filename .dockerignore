.git
.github
.idea
.vscode
node_modules
vendor
public/build
public/hot
public/storage
storage/logs/*
storage/framework/cache/data/*
storage/framework/sessions/*
storage/framework/views/*
database/database.sqlite
database/*.sqlite
.env
.env.*
!.env.example
.phpunit.result.cache
.phpunit.cache
tests
*.md

# build_for_git IS shipped in the image on purpose: the repository is private,
# so GitHub links 404 for players. The APK is served from this server instead
# (see the /download/apk route). Keep only ONE build in that folder — each one
# adds its full size to every image build.
